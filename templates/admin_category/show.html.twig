{% extends 'base-admin.html.twig' %}

{% block title %}Category{% endblock %}

{% block body %}
    <div class="container m-4">
        <div class="col-8">
            <h2 class="greenText mb-4 p-0">Information {{ category.label }}</h1>
            <table class="table">
                <tbody>
                    <tr>
                        <th>Id</th>
                        <td>{{ category.id }}</td>
                    </tr>
                    <tr>
                        <th>Label</th>
                        <td>{{ category.label }}</td>
                    </tr>
                </tbody>
            </table>
            <a class="btn btn-success mt-4" href="{{ path('app_admin_category_edit', {'id': category.id}) }}">modifier</a>
            {{ include('admin_category/_delete_form.html.twig') }}
            <a class="btn btn-success mt-4" href="{{ path('app_admin_category_index') }}">retour</a><br>
        </div>
    </div>
{% endblock %}
