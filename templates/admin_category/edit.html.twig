{% extends 'base-admin.html.twig' %}

{% block title %}Edit Category{% endblock %}

{% block body %}
    <div class="container m-4">
        <div class="col-8">
            <h2 class="greenText mb-4 p-0">Modifier catégories</h2>
            {{ include('admin_category/_form.html.twig', {'button_label': 'Modifier'}) }}
            {{ include('admin_category/_delete_form.html.twig') }}
            <a class="btn btn-success mt-4"href="{{ path('app_admin_category_index') }}">retour</a>
           
        </div>
    </div>
{% endblock %}
